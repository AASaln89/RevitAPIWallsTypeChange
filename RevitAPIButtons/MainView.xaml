<Window x:Class="RevitAPIWallsTypeChange.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RevitAPIWallsTypeChange"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <Label Content="Test buttons" Margin="5" Padding="5"/>
            <Button Content="Button1" Margin="5" Padding="5" Command="{Binding countPipes}"/>
            <Button Content="Button2" Margin="5" Padding="5" Command="{Binding volumeWalls}"/>
            <Button Content="Button3" Margin="5" Padding="5" Command="{Binding countDoors}"/>
        </StackPanel>
    </Grid>
</Window>
